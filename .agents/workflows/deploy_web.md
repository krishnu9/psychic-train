---
description: Build and deploy the application for web
---

1. Build the Flutter web app:
```bash
// turbo
flutter build web
```

2. Deploy the build output to Firebase Hosting:
```bash
// turbo
firebase deploy --only hosting
```
